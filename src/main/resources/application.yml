server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always


app:
  datasource:
    jdbc-url: jdbc:postgresql://localhost:5432/tomcatdb
    username: postgres
    password: password
    pool-size: 30

spring:
  security:
    filter:
      order: 10